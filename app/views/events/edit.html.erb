<% provide(:title, "Editar evento") %>
<% provide(:button_text, "Editar evento") %>
<h1><center><%= @event.title %></center></h1>

<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<%= render 'form', event: @event %>
		<center>
			<%= link_to 'Back', events_path %>
		</center>
	</div>
</div>
