default: &default
  adapter: postgresql
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: db/caccs_dev

test:
  <<: *default
  database: db/caccs_test

production:
  <<: *default
  database: db/caccs_prod
